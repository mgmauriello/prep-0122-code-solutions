function addTwoNumbers(x, y) {
  return x + y;
}

var sum = addTwoNumbers(4, 4);
console.log('sum', sum);

function convertHoursToMinutes(x) {
  return x * 60;
}

var convertHoursToMinutesResults = convertHoursToMinutes(3);
console.log('convert hours to minutes exercise', convertHoursToMinutesResults);

function getGreeting(x) {
  return x;
}
var getGreetingResults = getGreeting('World');
console.log('Hello', getGreetingResults);

function addAndMultiplyBy5(x, z) {
  return (x + z) * 5;
}

var addAndMultiplyBy5Results = addAndMultiplyBy5(3, 4);
console.log('add and divide by 5', addAndMultiplyBy5Results);

function multiplyAndDivideBy5(x, c) {
  return (x * c) / 5;
}

var multiplyAndDivideBy5Results = multiplyAndDivideBy5(6, 9);
console.log('multiply and divide by 5', multiplyAndDivideBy5Results);

function subtractTwoNumbers(x, z) {
  return x - z;
}

var subtractTwoNumbersResults = subtractTwoNumbers(30, 17);
console.log('subtract two numbers', subtractTwoNumbersResults);

function getCircleCircumference(z) {
  return 2 * 3.14159 * z;
}

var getCircleCircumferenceResults = getCircleCircumference(7);
console.log('circle circumference', getCircleCircumferenceResults);

function getFullName(x, z) {
  return x + z;
}

var getFullNameResults = getFullName('Boba', ' Fett');
console.log('full name', getFullNameResults);

function cube(x) {
  return x * x * x;
}

var cubeResults = cube(5);
console.log('cube', cubeResults);
